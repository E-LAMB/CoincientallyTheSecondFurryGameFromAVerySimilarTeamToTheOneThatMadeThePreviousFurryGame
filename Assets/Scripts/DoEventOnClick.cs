using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Events;

public class DoEventOnClick : MonoBehaviour
{/*

    public UnityEvent on_click;

    public bool needs_range;

    public LayerMask layers;

    public Player my_player;
    public bool check_for_bridge;

    private void OnMouseDown()
    {
        if (!needs_range)
        {
            on_click.Invoke();

        } else
        {
            if (my_player != null)
            {
                if (check_for_bridge && my_player.amount_stick == 5 && 
                Physics2D.OverlapCircle(gameObject.transform.position, 0.5f, layers))
                {
                    my_player.amount_stick = 0;
                    on_click.Invoke();
                }
            } else
            {
                if (Physics2D.OverlapCircle(gameObject.transform.position, 0.5f, layers))
                {
                    on_click.Invoke();
                }
            }
        }
    }
    
*/}
